# ???? CNN Baseline ???????

?????????????????? `CNNModel`??? checkpoint??????????? Botzone ???????????

???????? `model.py` ?? `CNNModel`?

```text
?? observation: 6 x 4 x 9
????: 235
???: CNNModel
checkpoint ??: PyTorch state_dict
????: __main__.py
```

## 1. ??????

?????

```text
agent.py          ?? Agent ?????
feature.py        FeatureAgent???????????? mask??????8 ??????
model.py          CNNModel ?????
preprocess.py     ???????????? npz ???
dataset.py        ?????? npz ??? PyTorch Dataset?
supervised.py     CNNModel ?????????
__main__.py       Botzone ????/?????
data/data.txt     ???????????
```

???????????

```text
data/count.json
data/0.npz
data/1.npz
...
checkpoints/cnn_model_epoch_N.pkl
checkpoints/cnn_model_latest.pkl
checkpoints/train_YYYYmmdd_HHMMSS.log
```

## 2. ????

?????

```text
numpy
torch
MahjongGB / PyMahjongGB
```

GPU ????????????????????`__main__.py` ??? CPU ?????

?????

```bash
python -c "import numpy, torch"
python -c "from MahjongGB import MahjongFanCalculator"
```

???????????? NumPy ? PyTorch?

???????????? PyMahjongGB???????? `MahjongFanCalculator`?

PyMahjongGB ?????

```text
https://github.com/ailab-pku/PyMahjongGB
```

## 3. ?????

?????????

```text
data/data.txt
```

????????????????????

??????

```bash
python preprocess.py
```

???????`data/` ???????

```text
data/count.json
data/0.npz
data/1.npz
data/2.npz
...
```

???? `data/count.json` ? `data/*.npz`??????????

## 4. ?? CNNModel

???????

```bash
python supervised.py --data-dir data --output-dir checkpoints --epochs 20 --batch-size 1024
```

CPU ?????

```bash
python supervised.py --device cpu --data-dir data --output-dir checkpoints --epochs 20 --batch-size 1024
```

?????????

```bash
python supervised.py --data-dir data --output-dir checkpoints --epochs 1 --batch-size 512
```

?????

```text
--data-dir        ?????????? count.json ? *.npz???: data
--output-dir      checkpoint ????????????: checkpoints
--epochs          ???????: 20
--batch-size      batch size???: 1024
--lr              ??????: 5e-4
--split-ratio     ? match ????/?????????: 0.9
--num-workers     DataLoader workers???: 0
--device          cuda ? cpu???: ? CUDA ? cuda??? cpu
--log-interval    ????? batch ?????????: 128
--log-file        ???????????????
```

## 5. ????

???????????????????

?????????

```text
checkpoints/train_YYYYmmdd_HHMMSS.log
```

???????

```bash
python supervised.py --data-dir data --output-dir checkpoints --log-file logs/cnn_baseline.log
```

???????

```text
2026-06-07 12:00:00 | INFO | [Log] file=checkpoints/train_20260607_120000.log
2026-06-07 12:00:00 | INFO | [Config] model=CNNModel data_dir=data output_dir=checkpoints epochs=20 batch_size=1024 lr=0.0005 split_ratio=0.9000 num_workers=0 device=cuda seed=2026 log_interval=128
2026-06-07 12:00:00 | INFO | [Data:train] begin=0.0000 end=0.9000
2026-06-07 12:00:00 | INFO | [Data:train] loading_match=0/88388
2026-06-07 12:03:21 | INFO | [Train] epoch=1 iter=128/500 samples=132096 loss_avg=2.345678 top1_avg=0.3521 samples_per_sec=4210.55 elapsed_sec=31.4
2026-06-07 12:10:00 | INFO | [Epoch] done epoch=1/20 train_loss=2.123456 train_top1=0.3812 valid_loss=2.234567 valid_top1=0.3621 valid_top3=0.6210 valid_top5=0.7043 train_sec=520.4 valid_sec=42.1
2026-06-07 12:10:00 | INFO | [Checkpoint] epoch=checkpoints/cnn_model_epoch_1.pkl latest=checkpoints/cnn_model_latest.pkl
```

????????

```text
train_loss        ??????????
train_top1        ??? top-1 ?????
valid_loss        ??????????
valid_top1        ??? top-1 ?????
valid_top3        ??? top-3 ?????
valid_top5        ??? top-5 ?????
samples_per_sec   ????
```

????????????????????????????????????

## 6. Checkpoint ??

?? epoch ????????

```text
checkpoints/cnn_model_epoch_N.pkl
checkpoints/cnn_model_latest.pkl
```

???

```text
cnn_model_epoch_N.pkl     ? N ? epoch ?????
cnn_model_latest.pkl      ??????????????
```

checkpoint ??? `CNNModel.state_dict()`?? `__main__.py` ????????

```python
model = CNNModel()
model.load_state_dict(torch.load(model_path, map_location=torch.device('cpu')))
```

???????

```text
checkpoints/cnn_model_latest.pkl
```

## 7. ????

??????

```text
__main__.py
```

???? `CNNModel`???????? Botzone ??????? `FeatureAgent` ???????????????????

???? checkpoint?

```bash
python __main__.py
```

?? checkpoint ???

```text
checkpoints/cnn_model_latest.pkl
```

???? checkpoint?

```bash
python __main__.py checkpoints/cnn_model_latest.pkl
```

???????? checkpoint?

```powershell
$env:MODEL_PATH="checkpoints/cnn_model_latest.pkl"
python __main__.py
```

???????

```text
PASS
PLAY XX
HU
GANG XX
BUGANG XX
```

`CNNModel` ?????? `action_mask` ????????????????????

## 8. ??????

`supervised.py` ????

```text
train_loss
train_top1
valid_loss
valid_top1
valid_top3
valid_top5
```

?????

```text
valid_loss ??        ????????????????
valid_top1 ??        ?????????????????
valid_top3/top5 ??   ???????????????????
??/??????      ?????
top1 ?? top5 ??    ??????????????????
```

?????????????????????????????????????????????

## 9. ??????

???????????? arena?judge ? self-play ?????

???????

```text
supervised.py   ????????
__main__.py     Botzone ???????
```

?????????????????????

### ?? A?????? Botzone ????????

????????? judge/arena?????????

```bash
python __main__.py checkpoints/cnn_model_latest.pkl
```

?????????

```text
??
???
????
???
?????
??????
????
????
??????
```

?????

```text
?????? bot
????? bot
? CNN baseline
?? CNNModel
????????
```

### ?? B??????? arena

?????? judge????????? arena????????

```text
???????
??
?????????????????
??? bot ?? Botzone ????
?? bot ??
???????
?? MahjongFanCalculator ?? 8 ???
??????????????????
```

????????????????????????????

## 10. ?????????

??????????????

```bash
python -c "import numpy, torch"
python -c "from MahjongGB import MahjongFanCalculator"
python preprocess.py
python supervised.py --data-dir data --output-dir checkpoints --epochs 1 --batch-size 512
python __main__.py checkpoints/cnn_model_latest.pkl
```

???????????????? judge ? Botzone ????????????

## 11. ????

`ModuleNotFoundError: No module named 'numpy'`

?? Python ???? NumPy????? NumPy?

`MahjongGB library required!`

????? PyMahjongGB????????????

```bash
python -c "from MahjongGB import MahjongFanCalculator"
```

`FileNotFoundError: data/count.json`

???????????????

```bash
python preprocess.py
```

`FileNotFoundError: checkpoints/cnn_model_latest.pkl`

?????????? checkpoint ?????????????????? checkpoint?

```bash
python __main__.py path/to/checkpoint.pkl
```

??? CUDA ?????

?? batch size?

```bash
python supervised.py --batch-size 256
```

???? CUDA?

???? CPU?

```bash
python supervised.py --device cpu
```

????????????

??? `valid_top1`?`valid_top3`?`valid_top5`???????????? CNN baseline ??????????????

## 12. ?????????

?? baseline ??????????????

```text
????????????????
????????????????????????
????????????
?? fan-route ?? head
?? afterstate value reranking
???????? arena
```

?????

```text
???? arena / judge wrapper
?? per-action validation metrics
?? illegal-action-rate ??
?????????????
? CNN baseline ?????????? ResNet-Style Tile Encoder
```
