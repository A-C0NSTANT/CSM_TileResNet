# ???? CNN Baseline ????

????????? AI agent ??????????????????????? validation ????????? BotZone smoke test ????? evaluation?

???????????? arena/judge??????????????????????

## 1. ???????

???????

```text
model.py
```

????

```text
CNNModel
```

?? checkpoint?

```text
checkpoints/cnn_model_latest.pkl
```

???

```text
observation shape: B x 6 x 4 x 9
action_mask shape: B x 235
```

???

```text
masked logits shape: B x 235
```

???`CNNModel.forward()` ?????? `action_mask`????? logits ????????

## 2. CNNModel ????

?? `CNNModel` ?????

```text
Input: B x 6 x 4 x 9

Conv2d(6, 64, kernel_size=3, stride=1, padding=1, bias=False)
ReLU(inplace=True)

Conv2d(64, 64, kernel_size=3, stride=1, padding=1, bias=False)
ReLU(inplace=True)

Conv2d(64, 64, kernel_size=3, stride=1, padding=1, bias=False)
ReLU(inplace=True)

Flatten

Linear(64 * 4 * 9, 256)
ReLU

Linear(256, 235)

Action mask

Output: B x 235
```

????

```text
total params: 727,659
trainable params: 727,659
```

??????

```text
conv1: 6 * 64 * 3 * 3 = 3,456
conv2: 64 * 64 * 3 * 3 = 36,864
conv3: 64 * 64 * 3 * 3 = 36,864
fc1: 64 * 4 * 9 * 256 + 256 = 590,080
fc2: 256 * 235 + 235 = 60,395
total: 727,659
```

????

```text
Conv2d / Linear weight ?? kaiming_normal_
Linear bias ?? PyTorch ?????
```

## 3. ??????

?? observation ? `feature.py` ?? `FeatureAgent` ???

?????

```text
OBS_SIZE = 6
ACT_SIZE = 235
observation = 6 x 4 x 9
```

?? observation ?????

```text
seat wind
prevalent wind
self hand count encoded by 4 hand planes
```

???

```text
??????????????????
????????????????
????????????????
????????????
```

?? CNN baseline ??? accuracy ????????????????

## 4. ??????

???????

```text
235
```

?? offset ?? `feature.py`?

```text
Pass:   0
Hu:     1
Play:   2   - 35    ? 34 ?
Chi:    36  - 98    ? 63 ?
Peng:   99  - 132   ? 34 ?
Gang:   133 - 166   ? 34 ?
AnGang: 167 - 200   ? 34 ?
BuGang: 201 - 234   ? 34 ?
```

??????????????????

```python
def action_type(action):
    if action == 0:
        return 'Pass'
    if action == 1:
        return 'Hu'
    if 2 <= action < 36:
        return 'Play'
    if 36 <= action < 99:
        return 'Chi'
    if 99 <= action < 133:
        return 'Peng'
    if 133 <= action < 167:
        return 'Gang'
    if 167 <= action < 201:
        return 'AnGang'
    if 201 <= action < 235:
        return 'BuGang'
    return 'Invalid'
```

???????????????????????

```text
??: Pass, Hu, Play, Chi, Peng, Gang, AnGang, BuGang
??: Pass, Hu, Play, Chi, Peng, AnyGang
```

???

```text
AnyGang = Gang + AnGang + BuGang
```

## 5. ??????

?????????

```text
evaluation/eval_supervised.py
```

?????????????????????????

```text
eval_supervised.py
```

?????

```text
1. ???????
2. ?? CNNModel checkpoint
3. ???? supervised metrics
4. ?? per-action-type metrics
5. ?? illegal action rate
6. ??????????????
7. ????????????? debug
```

????

```text
????????? arena
???? counterfactual ????
?????????????
???? BotZone ??????????
```

## 6. ???????

?????????

```bash
python evaluation/eval_supervised.py \
  --data-dir data \
  --checkpoint checkpoints/cnn_model_latest.pkl \
  --split-begin 0.9 \
  --split-end 1.0 \
  --batch-size 1024 \
  --device cpu \
  --output-dir evaluation/results/cnn_baseline_v0
```

?????

```text
--data-dir          ?????????? count.json ? *.npz
--checkpoint        ??? checkpoint
--split-begin       ????? match ????? 0.9
--split-end         ????? match ????? 1.0
--batch-size        batch size??? 1024
--device            cpu ? cuda???????
--output-dir        ????????
--max-samples       ???????? smoke evaluation
--save-failures     ????????
--topk              top-k ????? 1,3,5
```

## 7. ??????

### 7.1 ????

?????

```text
num_samples
loss
top1_accuracy
top3_accuracy
top5_accuracy
illegal_action_rate
label_illegal_rate
avg_legal_actions
avg_entropy
```

???

```text
loss: masked cross entropy
top1_accuracy: argmax ????????
top3/top5_accuracy: ??????? top-k ???
illegal_action_rate: ???????????? action_mask ?????? 0
label_illegal_rate: ???????? action_mask ?????? 0
avg_legal_actions: ????????????
avg_entropy: masked softmax ????????????????????
```

### 7.2 ???????

??? action type ???

```text
support
top1_accuracy
top3_accuracy
top5_accuracy
recall
pred_count
precision
```

?????

```text
Pass
Hu
Play
Chi
Peng
Gang
AnGang
BuGang
AnyGang
```

?????

```text
Hu recall
Peng recall
Chi recall
AnyGang recall
Play top5 accuracy
Pass precision
```

????? accuracy ?????? `Play` ? `Pass` ???????? `Hu`?`Peng`?`Gang` ?????????????????

### 7.3 ????

???? action type ????????

????????????????????

```text
label_type -> pred_type
```

???

```text
          Pass    Hu    Play    Chi    Peng    AnyGang
Pass      9200    0     0       12     31      2
Hu        8       92    0       0      0       0
Play      0       0     52000   0      0       0
Chi       340     0     0       220    18      0
Peng      610     0     0       15     310     4
AnyGang   95      0     0       0      8       44
```

????????????????? `Pass` ? `Play`?

### 7.4 ?????????

??? `action_mask.sum()` ????????

```text
legal_count = 2
legal_count = 3-5
legal_count = 6-10
legal_count > 10
```

??????????????????????

### 7.5 Hu ????

???????

```text
hu_label_count: label == Hu ????
hu_label_top1: label == Hu ??? top1 ?? Hu
hu_label_top3: label == Hu ? Hu ??? top3
hu_available_count: action_mask[Hu] == 1 ????
hu_available_pred_hu_rate: Hu ??????? Hu ???
hu_available_top3_hu_rate: Hu ??? Hu ???? top3
```

????????????? 8 ??`FeatureAgent` ???? `MahjongFanCalculator` ???? Hu??? Hu ?????????????

## 8. ????????

???????

```text
1. ???????
2. ?? output-dir
3. ?? console + file logging
4. ?? MahjongGBDataset(split_begin, split_end, data_dir)
5. ?? DataLoader
6. ?? CNNModel
7. ?? checkpoint
8. model.eval()
9. ????? batch
10. ?????? masked logits
11. ?? loss/top-k/illegal rate
12. ?? action type ??
13. ??????
14. ????????
15. ?? summary ????
16. ?? results.json
17. ?? confusion_matrix.csv
18. ?? failures.jsonl
```

## 9. ??????

??????????????

```text
evaluation/results/cnn_baseline_v0/
  eval.log
  results.json
  action_type_metrics.csv
  confusion_matrix.csv
  legal_count_buckets.csv
  failures.jsonl
```

`results.json` ???

```json
{
  "model": "CNNModel",
  "checkpoint": "checkpoints/cnn_model_latest.pkl",
  "num_samples": 123456,
  "loss": 2.345678,
  "top1_accuracy": 0.3521,
  "top3_accuracy": 0.6123,
  "top5_accuracy": 0.7043,
  "illegal_action_rate": 0.0,
  "label_illegal_rate": 0.0,
  "avg_legal_actions": 8.42,
  "avg_entropy": 1.93,
  "hu_label_count": 120,
  "hu_label_top1": 0.7667,
  "hu_available_count": 180,
  "hu_available_pred_hu_rate": 0.7111
}
```

`action_type_metrics.csv` ?????

```text
action_type,support,pred_count,precision,recall,top1,top3,top5
```

`failures.jsonl` ???????

```json
{
  "sample_index": 123,
  "label": 99,
  "label_type": "Peng",
  "pred": 0,
  "pred_type": "Pass",
  "legal_count": 4,
  "top5": [0, 99, 1, 133, 36],
  "top5_types": ["Pass", "Peng", "Hu", "Gang", "Chi"],
  "label_prob": 0.21,
  "pred_prob": 0.48
}
```

## 10. ??????

### 10.1 ????

??????????????????

```python
from model import CNNModel

model = CNNModel().to(device)
state_dict = torch.load(args.checkpoint, map_location=device)
model.load_state_dict(state_dict)
model.eval()
```

### 10.2 ????

`CNNModel` ??????????

```python
input_dict = {
    'is_training': False,
    'obs': {
        'observation': obs.to(device),
        'action_mask': mask.to(device),
    }
}
```

### 10.3 top-k ??

?? `CNNModel` ???? masked???? logits ? top-k?

```python
pred_topk = logits.topk(k, dim=1).indices
topk_correct = pred_topk.eq(target.view(-1, 1)).any(dim=1)
```

### 10.4 illegal action rate

?????? mask????????

```python
pred = logits.argmax(dim=1)
illegal = mask[torch.arange(batch_size), pred] <= 0
illegal_action_rate = illegal.float().mean()
```

?????????

```text
0.0
```

???? 0??? mask?????????????????

### 10.5 label illegal rate

????????????????

```python
label_illegal = mask[torch.arange(batch_size), target] <= 0
```

???????? 0?

??? 0????? `preprocess.py` ???????? mask ????

### 10.6 entropy

?????????????

```python
probs = torch.softmax(logits, dim=1)
entropy = -(probs * torch.log(probs.clamp_min(1e-12))).sum(dim=1)
```

?? entropy ????????????? entropy ???????????????

## 11. Debug Case ??

???????

```text
evaluation/extract_debug_cases.py
```

??? case ????? `eval_supervised.py`?

?????

```text
label == Hu ? pred != Hu
Hu legal ? pred != Hu
label == Peng ? pred == Pass
label == Chi ? pred == Pass
label in Gang/AnGang/BuGang ? pred != label
top1 wrong ? pred_prob > 0.8
label ?? top5
legal_count > 10 ?????
```

??????????????

```text
max_cases_per_type = 100
```

?? case ???????????????

## 12. BotZone Smoke Test

BotZone ????????????????????????

????

```text
?????
Bot ?????
Bot ???
Bot ???
Bot ???????
Bot ???????
```

???

```text
BotZone match id
????
?? Invalid
?? timeout
????
??????
```

?? BotZone ?????? smoke test????????????

## 13. ???? Evaluation

???????? arena/judge????? evaluation ???????

?????

```text
1. ????????? judge
2. ? BotZone ??????? bot ????
3. ?????? arena
4. ?????? Chinese-Standard-Mahjong ?????
```

?? bot ???

```text
RandomLegalBot
PassThenRandomDiscardBot
SimpleHeuristicBot
CNNBaselineBot
FutureResNetBot
```

???????

```text
win_rate
average_score
average_rank
deal_in_rate
hu_rate
valid_hu_rate
illegal_action_count
crash_count
timeout_count
average_decision_latency
```

?????

```text
20 games: smoke-level only
100 games: weak but useful signal
500+ games: more reliable comparison
```

## 14. ???????

???? ResNet-Style Tile Encoder ? Res2Net-Lite Tile Encoder ????????????????

?????

```text
CNNModel baseline
ResNet-style tile encoder
Res2Net-lite tile encoder
CNNModel + improved features
ResNet + improved features
```

????????

```text
?? validation split
?? checkpoint ????
?? batch size ???????
?? metrics
?? action space
?? action mask ??
```

???????

```text
parameter count
validation loss
top1/top3/top5
per-action metrics
illegal action rate
decision latency
BotZone smoke test result
?????????? judge ??????
```

## 15. ?????????

?????????? CNN baseline ????????

```text
forward pass ??
checkpoint ???
illegal_action_rate = 0
label_illegal_rate = 0
valid_loss ????? CNN baseline
valid_top3/top5 ????? CNN baseline
Hu/Peng/Chi/Gang ??????????????
BotZone smoke test ????????? Invalid
```

?????????????????????????????

```text
????? supervised validation ? BotZone smoke test???????? game-level evaluation?
```

## 16. ??????

?????????????

```text
1. ?? evaluation/eval_supervised.py
2. ???? loss/top1/top3/top5/illegal rate
3. ?? action type mapping
4. ?? per-action-type metrics
5. ?? confusion matrix
6. ?? Hu ????
7. ?? legal-count bucket ??
8. ?? results.json ? csv ??
9. ?? failures.jsonl
10. ? CNN baseline checkpoint ???????
11. ????? cnn_baseline_v0 ?????
```

?????????????? arena ????????
